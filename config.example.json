{
  "_comment": "Non-secret operational config for sgv-tweet-digest. Copy this file to your real config (e.g. ~/.config/sgv-tweet-digest/config.json) and fill in YOUR values. Secrets do NOT belong here; they live in your .env (see .env.example). Your real config copy is gitignored.",

  "_accounts_comment": "The two X accounts you draft for. personal = the 'mist' voice (your handle); sgv = the 'sgv' fund voice. Resolve numeric user ids at e.g. https://tweeterid.com or via the X API users/by/username endpoint.",
  "personal_user_id": "YOUR_PERSONAL_X_USER_ID",
  "personal_username": "YOUR_PERSONAL_X_HANDLE",
  "sgv_user_id": "YOUR_FUND_X_USER_ID",
  "sgv_username": "YOUR_FUND_X_HANDLE",

  "_followed_list_ids_comment": "X Lists whose tweets Stage A gathers (read via OAuth2 user token against YOUR @handle). Each entry: numeric list id + a human label + the list owner's handle. Replace these examples with the Lists you actually follow. A List id is the number in https://x.com/i/lists/<id>.",
  "followed_list_ids": [
    {
      "id": "0000000000000000001",
      "name": "Example Crypto VC List",
      "owner": "example_list_owner"
    },
    {
      "id": "0000000000000000002",
      "name": "Example Founders List",
      "owner": "another_list_owner"
    }
  ],

  "_membership_allowlist_comment": "Optional name-substring allowlist used when snapshotting which Lists you belong to. Leave empty [] to include all, or list label fragments to keep only matching Lists.",
  "membership_allowlist": [
    "Example Topic A",
    "Example Topic B"
  ],

  "_models_comment": "Anthropic model ids the pipeline calls. Opus does the high-stakes scoring + draft selection; Sonnet does the insights pass, good-content drafting, and feedback profile. Override here if you want different model versions; the ANTHROPIC_API_KEY itself lives in .env.",
  "models": {
    "opus": "claude-opus-4-1",
    "sonnet": "claude-sonnet-4-5"
  },

  "_budget_comment": "Cost guardrails. daily_x_cost_cap_usd is the hard ceiling Stage A (gather_x.py) enforces on paid X API spend per day. anthropic_budget_usd is the soft per-run Anthropic spend the digest aims to stay under.",
  "daily_x_cost_cap_usd": 1.0,
  "anthropic_budget_usd": 0.5,

  "_scoring_comment": "Heuristic scoring knobs. score_threshold = minimum 0-18 heuristic score for a gathered tweet to be considered for the top-3 engage list. candidate_count = how many top-scoring candidates to hand to Opus for the final pick.",
  "score_threshold": 8,
  "candidate_count": 30
}
